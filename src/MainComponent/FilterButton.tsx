import { Button } from '../components/ui/button'

const FilterButton = () => {
  return (
   <>
   <Button className='w-[12rem] h-[3rem] bg-[white] hover:bg-transparent' variant={"outline"} > Filter</Button>
   </> 
  )
}

export default FilterButton